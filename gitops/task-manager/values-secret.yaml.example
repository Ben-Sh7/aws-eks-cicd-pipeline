# Example only - do not commit real credentials.
# For local/demo use: copy to values-secret.yaml (gitignored), fill in real
# values, then: helm upgrade --install task-manager gitops/task-manager -f values-secret.yaml

secrets:
  mode: local
  dbUser: postgres
  dbPassword: your-secure-password-here
